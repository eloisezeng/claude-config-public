<!-- Global (cross-project) memory index. Pointer lines only — one per PURE-FACT
     or preference memory. Bodies are sibling <name>.md files. Loaded into every
     session by inject-global-memory.mjs (SessionStart hook). Keep this lean.
     Behavioral directives are NOT indexed here — their one-line imperatives live
     in CLAUDE.md ## Working directives (indexing them twice double-loads every
     session); their bodies still live in this directory. -->

- [background-fleet-memory-budget](background-fleet-memory-budget.md) — a bg fleet costs ~1 GB/session idle and grows with CONTEXT not count; daemon is already PPID 1; recover crash-killed sessions via reapedMidWorkAt
- [a-deleted-bg-job-survives-in-its-transcript](a-deleted-bg-job-survives-in-its-transcript.md) — deleting a bg job kills the process and removes job dir, tmp and worktree, but the transcript + rewind snapshots survive: rebuild, replay the writes, `claude --bg --resume` from the ORIGINAL cwd on `opus[1m]`; the 8-hex id is kept ONLY when the daemon fully released it, else a NEW id and the phantom `running pid=None` record must be retired by hand; one launch per line (a leaked `cd` boots a seat in the wrong worktree)
- [gui-launched-tools-get-no-shell](gui-launched-tools-get-no-shell.md) — a VS Code profile/LaunchAgent/.app runs with NO shell: PATH is launchd's bare 4 dirs, the tab closes before the error shows; resolve binaries absolutely and never exit silently
- [claude-agents-is-the-fleet-write-surface](claude-agents-is-the-fleet-write-surface.md) — `claude agents` IS the write surface for running sessions: arrow to one, SPACE to reply; it gates on folder trust so never launch it from $HOME
- [background-subagent-parallel-workflow](background-subagent-parallel-workflow.md) — fire off messages; have the heavier ones answered by background subagents in parallel
- [budget-reservations-cover-call-multiplicity](budget-reservations-cover-call-multiplicity.md) — metered paths need reservations covering REAL call multiplicity (retries/resumes/fan-out), released only on commit
- [conflicted-pr-gets-no-ci](conflicted-pr-gets-no-ci.md) — a PR conflicting with its base runs NO CI and reports "no checks configured"; check mergeable_state first
- [brainstorm-in-lavish](brainstorm-in-lavish.md) — conduct brainstorming/design Q&A through lavish-axi, not terminal AskUserQuestion
- [check-memory-before-asking-user](check-memory-before-asking-user.md) — search existing memory before asking the user to re-supply context
- [claude1-second-profile](claude1-second-profile.md) — how the `claude1` command launches a second Claude Code profile separate from default `claude`
- [cloud-synced-edits-bypass-dev-server-watcher](cloud-synced-edits-bypass-dev-server-watcher.md) — in a OneDrive/Dropbox folder the dev server misses sync-delivered edits and serves a stale bundle; restart it
- [codify-as-rules-not-skills](codify-as-rules-not-skills.md) — prefer always-on instructions over invoke-when-remembered skills; test against a no-guidance control first
- [dev-pipeline-plan-subagent-converge](dev-pipeline-plan-subagent-converge.md) — default pipeline: brainstorm/spec → writing-plans → subagent impl → Claude↔Codex converge
- [feedback-privacy-business-material](feedback-privacy-business-material.md) — be privacy-conscious with business material; confirm before any outward-facing publish
- [zsh-agent-shell-quoting-gotchas](zsh-agent-shell-quoting-gotchas.md) — zsh does NOT word-split unquoted expansions (a multi-line $FILES is ONE arg, so `vitest run $FILES` exits 0 having run nothing); BSD grep has no `\|`; grep needs `-a` on NUL-bearing files; strip ANSI before grepping
- [global-config-repo](global-config-repo.md) — global Claude config is a symlinked private dotfiles repo with cross-machine auto-sync (Windows uses copies, not symlinks)
- [github-desktop-autostashes-on-branch-switch](github-desktop-autostashes-on-branch-switch.md) — silently stashes edits on branch switch; "files vanished" → check `git stash list` first
- [github-personal-repo-collaborators-are-always-write](github-personal-repo-collaborators-are-always-write.md) — every collaborator on a user-owned repo has write regardless of role; read-only needs an org
- [grill-defer-domain-judgment](grill-defer-domain-judgment.md) — defer to Claude's judgment on niche industry-convention details; reserve questions for product/scope decisions
- [instagram-oauth-professional-only-no-email](instagram-oauth-professional-only-no-email.md) — professional accounts only, never returns an email — offer "connect", not "sign in"
- [latex-math-in-docs](latex-math-in-docs.md) — write math as LaTeX `$...$` in markdown docs and matplotlib mathtext in figures; never bare unicode/ASCII
- [lavish-artifact-prefs](lavish-artifact-prefs.md) — how to build lavish-axi review artifacts — interactive radios, working submit, notifications
- [lavish-axi-fork](lavish-axi-fork.md) — lavish-axi must be the user's fork (~/code/lavish-axi-fork, npm-linked), not upstream
- [lavish-axi-threading-and-collapsibles](lavish-axi-threading-and-collapsibles.md) — `stream --reply-to` for threads; no `<details>`; RESTART the server for fork build changes
- [lavish-no-redisplay-answered-questions](lavish-no-redisplay-answered-questions.md) — in lavish surfaces, never re-display a question the user already answered
- [mark-fixture-data](mark-fixture-data.md) — mark placeholder/stub data shaped like real data with a FIXTURE marker
- [mermaid-id-collision-lavish](mermaid-id-collision-lavish.md) — v11 startOnLoad same-ms SVG ids collide in multi-diagram artifacts; render each with unique ids
- [parse-xlsx-with-claude-for-excel](parse-xlsx-with-claude-for-excel.md) — use the Claude for Excel add-in to parse .xlsx files, not stdlib XML
- [playwright-forcedcolors-fixture-noops](playwright-forcedcolors-fixture-noops.md) — `test.use({ forcedColors })` silently no-ops; use page.emulateMedia, assert the mode is ON as line one
- [playwright-pins-page-visibility](playwright-pins-page-visibility.md) — visibilityState is pinned "visible" on every driven page; test hidden-tab behaviour in unit tests instead
- [sqlite-prepare-resolves-tables-at-prepare-time](sqlite-prepare-resolves-tables-at-prepare-time.md) — table AND index names resolve at prepare(), before any guard; an unconditional INDEXED BY throws
- [sqlite-rowid-is-reused-after-delete](sqlite-rowid-is-reused-after-delete.md) — a deleted row's id is reused, so delete-then-insert can collide with a stale writer's snapshot; insert BEFORE delete
- [present-options-abc-not-star](present-options-abc-not-star.md) — label choice options A/B/C; mark the recommendation with "(recommended)" text, not a star
- [detached-work-dies-with-its-allocation](detached-work-dies-with-its-allocation.md) — teardown kills the whole cgroup (setsid/nohup don't escape); durable agents = tmux on a login node, work = batch job
- [verify-the-bottleneck-before-building-the-fix](verify-the-bottleneck-before-building-the-fix.md) — measure which stage actually binds first; the plausible first diagnosis is routinely the wrong one
- [visualize-in-browser](visualize-in-browser.md) — "visualize" for web/UI → lavish-axi (not a browser tab); scientific figures → matplotlib
- [namespace-run-artifacts-per-arc](namespace-run-artifacts-per-arc.md) — parallel arcs share one scratchpad; generic run-log names clobber silently and the round then proves nothing
- [mocks-blind-to-the-mechanism-they-replace](mocks-blind-to-the-mechanism-they-replace.md) — a mock can't see behaviour the mocked thing implements (redirects, retries); enforce at a choke point
- [context-mode-webfetch-blocks-artifact-reads](context-mode-webfetch-blocks-artifact-reads.md) — its interceptor can't read claude.ai artifacts; verify base via git-object provenance, then force-publish
- [max-20x-subscription-context-discipline](max-20x-subscription-context-discipline.md) — Fable is the accepted default; window size/session length are the burn lever — obey context-watchdog nudges
- [macos-find-is-bfs-no-relative-newermt](macos-find-is-bfs-no-relative-newermt.md) — bfs `find`: `-newermt "-20 minutes"` silently ERRORs, use `-mtime -20m`; test absence-probes against known presence
- [hf-personal-repos-have-no-collaborators](hf-personal-repos-have-no-collaborators.md) — a personal-namespace repo can't grant write; probe `user-access-request/pending`, never `preupload`
- [config-sync-commits-onto-detached-head](config-sync-commits-onto-detached-head.md) — an interrupted rebase makes the config auto-sync commit git conflict MARKERS as content into the always-loaded CLAUDE.md/MEMORY.md; resolve as a union, never `rebase --abort`
- [cross-session-inbound-hold-is-mode-parity](cross-session-inbound-hold-is-mode-parity.md) — "Cross-session message held for approval" is Claude Code's mode-parity inbound gate; `crossSessionInbound: accept` in USER settings (repo scope can only tighten) delivers them automatically
- [cluster-find-is-bfs-use-mmin](cluster-find-is-bfs-use-mmin.md) — on the your-university cluster `find` is `bfs`: relative `-newermt` strings fail silently to EMPTY; use `-mmin -N`; test a liveness probe against a known-fresh file before trusting its silence
- [a-session-cannot-see-its-own-stall](a-session-cannot-see-its-own-stall.md) — a rate-limit stall writes NO turn, so "I never stopped" is unobservable from inside; measure wall-clock gaps between transcript timestamps (measured: a 38.6-min hole invisible in my own context)
- [saturation-claims-need-a-suffix-not-a-first-hit](saturation-claims-need-a-suffix-not-a-first-hit.md) — "beyond N, more buys nothing" is a SUFFIX over the searched range, not the first N at the floor; compute floorStableAt by scanning DOWN from the end (measured: worst Playwright leg is non-monotone in shard count)
- [a-probe-is-part-of-the-population-it-measures](a-probe-is-part-of-the-population-it-measures.md) — your own command is a member of any live population you `ps`/grep; controls need the measurement's exact predicate AND breadth, split the target literal, and a control reading NON-ZERO invalidates just as a positive control reading zero does
- [retraction-goes-where-the-instruction-is](retraction-goes-where-the-instruction-is.md) — a correction appended at the tail of a long record retracts nothing; put it at each wrong instruction, append-only, and fix every site of the class
- [session-reachability-is-status-not-state](session-reachability-is-status-not-state.md) — reachable ⟺ roster `status` non-null + socket; `state` is stale text; a limit death is REVIVABLE and a clean handoff also leaves the roster, so re-probe a spent session/tier and settle contested seats two-sidedly
- [flock-absent-on-macos-validate-boolean-probes](flock-absent-on-macos-validate-boolean-probes.md) — `flock(1)` does not exist on macOS: `flock -n F true` exits 127 so "non-zero = held" reports HELD for every input; prove a lease with perl `flock(LOCK_EX|LOCK_NB)` **plus a zeroing control**, and validate any boolean probe against controls that force both answers
- [bg-session-liveness-and-approval-parking](bg-session-liveness-and-approval-parking.md) — three probes that report a WORKING bg session as idle (`pgrep -P` can't see in-process subagents, byte growth isn't progress), and the approval-prompt park that no `SendMessage` can drain
- [kill-bg-claude-sessions-via-job-state](kill-bg-claude-sessions-via-job-state.md) — killing a bg session's PID doesn't stick: mark `~/.claude/jobs/<id>/state.json` `state=failed` FIRST; plus two handoff.sh dispatch bugs
- [a-retired-bg-seat-can-be-respawned-as-a-crash](a-retired-bg-seat-can-be-respawned-as-a-crash.md) — the bg daemon can read a handoff retirement as a crash and auto-restart the seat, flipping `state.json` from done back to working; a seat that wakes after its own handoff must stand down, not resume
- [gh-axi-rest-truncates-long-fields](gh-axi-rest-truncates-long-fields.md) — gh-axi's REST renderer silently truncates long string fields (~2KB), so any absence-check on a PR/issue body through it is structurally blind; use GraphQL
- [echo-fabricates-structure-in-zsh](echo-fabricates-structure-in-zsh.md) — the Bash tool runs zsh, whose builtin `echo` interprets backslash escapes and invents newlines/tabs that were never in the data; use `printf '%s\n'`
- [git-add-partial-failure-stages-the-rest](git-add-partial-failure-stages-the-rest.md) — a `git add` that errors on a gitignored path still stages every OTHER path in the same command, so the next commit silently carries files you thought were rejected
- [playwright-visibility-and-config-gotchas](playwright-visibility-and-config-gotchas.md) — three traps that make UI suites report green on broken pages: `opacity-0` counts as visible, emulated `reducedMotion` never runs the real transition, and the config is CJS so `import.meta` is a SyntaxError
- [a-pin-can-be-unpinned-without-being-touched](a-pin-can-be-unpinned-without-being-touched.md) — a test pinning a region or a self-reported count can be silently unpinned by an edit that never touches the test; enumerate the pins and re-anchor, never retarget
- [an-existential-release-rule-is-fail-open](an-existential-release-rule-is-fail-open.md) — a gate phrased "open when A review says clean" is fail-open the moment two reviews can cover the same artifact; quantify universally over the covering set
- [windows-hook-commands-avoid-env-vars](windows-hook-commands-avoid-env-vars.md) — on Windows with Git Bash, hook commands run through bash, so `$env:VAR` in a hook string is silently mangled; use literal absolute paths
- [zsh-eats-a-colon-suffix-after-an-unbraced-var](zsh-eats-a-colon-suffix-after-an-unbraced-var.md) — `$VAR:src/...` silently loses the suffix in zsh (history modifier on an unbraced expansion); quoting does not help, `${VAR}` does; `git show <rev>` with no path FAILS OPEN
- [git-checkout-is-not-a-restore](git-checkout-is-not-a-restore.md) — a mutation-control harness restoring with `git checkout --` reverts to the INDEX and silently eats uncommitted work; copy outside the repo, restore with `cp`, verify by hash, and make every control probe print a count either way
- [turbopack-rejects-a-symlinked-node-modules](turbopack-rejects-a-symlinked-node-modules.md) — a git worktree needs its OWN real node_modules; Turbopack refuses a symlinked one (and a symlink also lands outside a Codex workspace-write sandbox), so clone with `cp -c -R` on APFS and never replace the primary checkout's copy
- [a-merge-command-can-report-failure-after-succeeding](a-merge-command-can-report-failure-after-succeeding.md) — `gh-axi/gh pr merge` exits NON-ZERO from its LOCAL `--delete-branch` step while the server-side merge already landed; retrying on the exit code re-merges a merged PR, and where main auto-deploys that is a second production restart — read the wire, not the exit code
- [a-redirect-location-has-two-surfaces](a-redirect-location-has-two-surfaces.md) — a framework may PARSE a redirect's `Location` instead of forwarding it, so the handler's RETURN value (absolute, or `new URL()` throws) and the WIRE value (relative) differ; a unit test asserting one against the other CANNOT see the 500 and pins the bug — drive the real server, and verify live on the wire value
- [git-push-auth-is-osxkeychain-not-gh](git-push-auth-is-osxkeychain-not-gh.md) — on macOS `git push` reads ONE keychain entry per host via Apple's `osxkeychain` helper and never consults `gh`'s active account, so `gh auth switch` cannot fix a push and the human's own terminal fails identically to the agent's; select a second account with `https://<user>@github.com/<owner>/<repo>.git`
- [an-event-watch-needs-an-interval-backstop](an-event-watch-needs-an-interval-backstop.md) — an event watcher loses silently what it cannot see (macOS WatchPaths does not fire on in-place edits of a watched directory's files); pair it with an interval backstop, a DERIVED watch set asserted both directions, and an alert that outlives a vanishing toast
- [builtin-local-notifications-are-terminal-escapes-only](builtin-local-notifications-are-terminal-escapes-only.md) — Claude Code's built-in LOCAL notification channel emits only a terminal escape (OSC 9 / bell / OSC 99 / OSC 777), never an OS banner, and `auto` resolves to NOTHING outside Apple Terminal / iTerm2 / kitty / ghostty — VS Code included; `inputNeededNotifEnabled` / `agentPushNotifEnabled` are MOBILE push, so an osascript notification hook is a different surface, not a duplicate
- [interactive-sessions-default-to-bypass-permissions](interactive-sessions-default-to-bypass-permissions.md) — since 2026-09-01 ALL new sessions boot in `bypassPermissions` (her own `defaultMode`, set after the classifier refused agents doing it); do not hand-pass a mode that defeats it
- [a-permission-boundary-can-track-effect-not-command-text](a-permission-boundary-can-track-effect-not-command-text.md) — a refusal may track the EFFECT (mutating live production) rather than the command text, and it is STICKY: a form that passed BEFORE a write attempt gets refused after one, so record the ordering and never derive a "reads allowed, writes blocked" rule
- [chrome-devtools-axi-mcp-version-skew](chrome-devtools-axi-mcp-version-skew.md) — "Required at pageId" on every page-scoped command means the pinned MCP contract lags `chrome-devtools-mcp@latest`; the fix is `npm install -g chrome-devtools-axi@latest`, not abandoning the tool
- [a-compacted-fork-forgets-it-is-a-fork](a-compacted-fork-forgets-it-is-a-fork.md) — a fork that auto-compacts loses the fork-boilerplate and re-identifies as the PARENT, then duplicates the parent's arc (measured: one SendMessage to itself and one tool call from a second Codex panel); settle identity from the transcript FILE your tool calls land in (`subagents/agent-a*.jsonl` = fork → report once and stop), never from the summary
- [a-process-in-transition-has-no-argv](a-process-in-transition-has-no-argv.md) — between fork/exec and at exit teardown a process has NO readable argv: macOS `ps -o command` prints `(comm)` and `pgrep -f` matches nothing (measured 77 of 161 samples of a short sleep loop), so a predicate on command TEXT must accept that form or sample more than once; a zombie is `<defunct>`, a different state
- [a-conflicted-pr-gets-no-ci-run](a-conflicted-pr-gets-no-ci-run.md) — a conflicted PR has no merge ref so GitHub creates NO workflow run at all, silently, while attaching the branch's PREVIOUS green run to it; read `mergeable`/`mergeable_state` before blaming the token, an outage or a spending limit, and control an empty `?head_sha=` query with the branch listing
- [gh-setup-git-answers-with-the-active-account](gh-setup-git-answers-with-the-active-account.md) — `gh auth setup-git` installs ONE global credential helper answering as the ACTIVE account, shadowing every repo it cannot see; symptom is `Repository not found` or a silent push HANG.
- a-write-run-may-answer-with-a-design-and-stop — a codex `--write` lane can reply "please confirm this design" and write nothing while the launcher exits 0; ship an `<autonomy>` block and gate on COMMITS, since clean tree + zero commits reads as a no-op
